# Core AI
anthropic>=0.25.0

# Voice — TTS
elevenlabs>=1.0.0
pygame>=2.5.0
pyttsx3>=2.90          # fallback TTS when ElevenLabs keys not set

# Voice — STT / wake word
openai-whisper
pvporcupine
pyaudio
sounddevice
soundfile
numpy>=1.24.0

# UI
PyQt5>=5.15.0
pyautogui>=0.9.54

# Config / utils
python-dotenv>=1.0.0

# Volume control (Windows only)
pycaw>=20240210
comtypes>=1.3.0
